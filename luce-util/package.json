{
  "name": "luce-util",
  "version": "1.0.5",
  "description": "Common utilities for me",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && copy package.json dist",
    "publish": "cd dist && npm publish"
  },
  "author": "lucymair",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.1.2"
  },
  "files": [
    "dist"
  ]
}
